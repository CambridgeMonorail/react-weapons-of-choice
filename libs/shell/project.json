{
  "name": "shell",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shell/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project shell --web",
  "targets": {
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --project libs/shell/tsconfig.lib.json --noEmit"
      }
    }
  }
}
